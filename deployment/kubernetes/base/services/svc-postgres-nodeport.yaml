apiVersion: v1
kind: Service
metadata:
  name: postgres-nodeport
  namespace: investadvisor
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30543
      name: postgres
      protocol: TCP
  type: NodePort
