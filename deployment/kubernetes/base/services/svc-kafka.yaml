apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: investadvisor
  labels:
    app: kafka
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app: kafka
  ports:
    - port: 9092
      targetPort: 9092
      name: kafka
      protocol: TCP
