apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: investadvisor
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - port: 8084
      targetPort: 8084
      name: http
      protocol: TCP
  type: ClusterIP
