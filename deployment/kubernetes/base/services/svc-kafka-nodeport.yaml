apiVersion: v1
kind: Service
metadata:
  name: kafka-nodeport
  namespace: investadvisor
  labels:
    app: kafka
spec:
  selector:
    app: kafka
  ports:
    - port: 9092
      targetPort: 9092
      nodePort: 30094
      name: kafka
      protocol: TCP
  type: NodePort
