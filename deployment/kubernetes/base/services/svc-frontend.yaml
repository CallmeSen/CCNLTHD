apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: investadvisor
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      name: http
      protocol: TCP
  type: ClusterIP
