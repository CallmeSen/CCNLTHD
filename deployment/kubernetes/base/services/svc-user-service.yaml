apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: investadvisor
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 30100
      name: http
      protocol: TCP
  type: NodePort
