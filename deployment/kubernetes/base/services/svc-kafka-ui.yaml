apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: investadvisor
  labels:
    app: kafka-ui
spec:
  selector:
    app: kafka-ui
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30700
      name: http
      protocol: TCP
  type: NodePort
