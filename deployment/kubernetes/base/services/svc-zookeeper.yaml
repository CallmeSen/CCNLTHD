apiVersion: v1
kind: Service
metadata:
  name: zookeeper
  namespace: investadvisor
  labels:
    app: zookeeper
spec:
  clusterIP: None
  selector:
    app: zookeeper
  ports:
    - port: 2181
      targetPort: 2181
      name: zookeeper
      protocol: TCP
