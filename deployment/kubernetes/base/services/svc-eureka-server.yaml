apiVersion: v1
kind: Service
metadata:
  name: eureka-server
  namespace: investadvisor
  labels:
    app: eureka-server
spec:
  selector:
    app: eureka-server
  ports:
    - port: 8761
      targetPort: 8761
      nodePort: 30600
      name: eureka
      protocol: TCP
  type: NodePort
