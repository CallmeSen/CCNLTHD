apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: investadvisor
type: Opaque
# Template only. Do not commit real values here.
# Apply real values with:
#   powershell -ExecutionPolicy Bypass -File deployment/kubernetes/apply-env.ps1
stringData:
  JWT_SECRET: "REPLACE_ME"
  DB_PASSWORD: "REPLACE_ME"
  POSTGRES_MA_PASSWORD: "REPLACE_ME"
  DATABASE_URL: "REPLACE_ME"
  OPENROUTER_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  TAVILY_API_KEY: "REPLACE_ME"
  VNSTOCK_API_KEY: "REPLACE_ME"
  LANGCHAIN_API_KEY: "REPLACE_ME"

---
apiVersion: v1
kind: Secret
metadata:
  name: multi-agents-secrets
  namespace: investadvisor
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  OPENROUTER_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  TAVILY_API_KEY: "REPLACE_ME"
  SECRET_KEY: "REPLACE_ME"

---
apiVersion: v1
kind: Secret
metadata:
  name: mail-secrets
  namespace: investadvisor
type: Opaque
stringData:
  MAIL_USERNAME: "REPLACE_ME"
  MAIL_PASSWORD: "REPLACE_ME"
