spring:
  cloud:
    gateway:
      routes:
        - id: ai-intelligence-service
          uri: http://fin_multi_agents:8086
          predicates:
            - Path=/api/ai/**
          filters:
            - RewritePath=/api/ai/?(?<remaining>.*), /$\{remaining}
