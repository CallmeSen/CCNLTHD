# Environment variables for docker-compose
# Copy to .env and fill in real values before running docker compose.

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# JWT — CHANGE THIS to a random 32+ character string in production
JWT_SECRET=InvestAdvisorJwtSecretKeyMustBeAtLeast256BitsLong2024!

# vnstock API key — used by market-data-service (Python/FastAPI)
# Free key from https://vnstocks.com/login
# Without a key: 20 req/min (Guest tier). With a key: 60 req/min (Community tier).
# Leave blank to run as Guest.
VNSTOCK_API_KEY=

# Email (SMTP) — used by notification-service
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=noreply@yourdomain.com
MAIL_PASSWORD=your_app_password_here
